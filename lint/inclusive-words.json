{
  "words": [
    {
      "word": "guys",
      "suggestions": ["people"],
      "explanation": "The usage of the non-inclusive word '{{word}}' is discouraged, use '{{suggestion}}' instead."
    },
    {
      "word": "black-list",
      "suggestions": ["denylist"],
      "explanation": "The usage of the non-inclusive word '{{word}}' is discouraged, use '{{suggestion}}' instead."
    },
    {
      "word": "white-list",
      "suggestions": ["allowlist"],
      "explanation": "The usage of the non-inclusive word '{{word}}' is discouraged, use '{{suggestion}}' instead."
    },
    {
      "word": "sanity",
      "suggestions": ["confidence"],
      "explanation": "The usage of the non-inclusive word '{{word}}' is discouraged, use '{{suggestion}}' instead."
    },
    {
      "word": "dummy",
      "suggestions": ["placeholder", "sample"],
      "explanation": "The usage of the non-inclusive word '{{word}}' is discouraged, use '{{suggestion}}' instead."
    },
    {
      "word": "whitebox",
      "suggestions": ["open-box"],
      "explanation": "The usage of the non-inclusive word '{{word}}' is discouraged, use '{{suggestion}}' instead."
    },
    {
      "word": "blackbox",
      "suggestions": ["closed-box"],
      "explanation": "The usage of the non-inclusive word '{{word}}' is discouraged, use '{{suggestion}}' instead."
    }
  ]
}
